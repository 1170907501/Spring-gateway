server:
  port: 8888

#spring:
  #datasource:
 #   url: jdbc:mysql://localhost:3306/db_gupao?useSSL=false&serverTimezone=UTC
 #   username: root
    #password: root123
  #  driver-class-name: com.mysql.cj.jdbc.Driver

spring:
  # Spring Security 配置项，对应 SecurityProperties 配置类
  security:
    user:
      name: user # 用户名
      password: 123456 # 密码
      roles: # 拥有角色
        - admin
# 基础配置
  redis:
    host: localhost
    port: 6379
    password:  # 如有
    database: 0          # 默认库